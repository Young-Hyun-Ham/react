// Reactjs code snippets [ rccp ] 로 생성한 파일
import React, { Component } from 'react';
import PropTypes from 'prop-types';

class R023 extends Component {

    render() {
        return (
            <div style={{padding: "0px"}}>
                {/* 부모에서 코딩한 하위 노드를 가져온다. */}
                {this.props.children}
            </div>
        );
    }
}

export default R023;